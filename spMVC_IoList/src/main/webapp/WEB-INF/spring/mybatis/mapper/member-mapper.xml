<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.biz.iolist.mapper.MemberDao">
	<select id="selectAll" resultType="memberVO">
		SELECT * FROM tbl_member
	</select>
	<select id="findByUserid" resultType="memberVO">
		SELECT * FROM tbl_member WHERE m_userid = #{m_userid}
	</select>
	
	<insert id="insert" parameterType="memberVO">
		INSERT INTO tbl_member(m_id, m_userid, m_password, m_name, m_tel, m_addr) VALUES(SEQ_IO_MEMBER.NEXTVAL, #{m_userid}, #{m_password}, #{m_name}, #{m_tel}, #{m_addr})
	</insert>
	
	<update id="update" parameterType="memberVO">
		UPDATE tbl_member SET m_userid = #{m_userid}, m_password=#{m_password}, m_name=#{m_name}, m_tel=#{m_tel}, m_addr=#{m_addr} WHERE m_id = #{m_id} 
	</update>
	
	<delete id="delete">
		DELETE FROM tbl_member WHERE m_userid = #{m_userid}
	</delete>
</mapper>